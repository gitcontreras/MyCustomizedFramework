namespace MyCustomizedFramework.Application.{{ plural_name }};

public sealed record {{ table_name }}Request(
{{~ for column in insertable_columns ~}}
    {{ column.type }} {{ column.name }}{{ if !for.last }},{{ end }}
{{~ end ~}}
);
